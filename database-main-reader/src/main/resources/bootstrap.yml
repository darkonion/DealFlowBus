spring:
  application:
    name: database-main-reader
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: ovarkills11
      username: user
  jpa:    show-sql: true

  servlet:    multipart:      max-file-size: 100MB
      max-request-size: 110MB
      enabled: true                